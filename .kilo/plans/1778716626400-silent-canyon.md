# Plan: Review and Add Missing Translations

## Overview
Review all strings in the app and ensure they have both English and Spanish translations in the i18n locale files.

## Current State
- i18n is configured with `i18next` and `react-i18next`
- Two locale files exist: `src/locales/en.json` and `src/locales/es.json`
- Many strings are already translated
- Several hardcoded strings are missing translations

## Missing Strings by File

### 1. `src/components/add-edit-event-dialog.tsx`
| String | Context |
|--------|---------|
| "Event updated successfully" | toast.success |
| "Event created successfully" | toast.success |
| "Edit Event" | Modal title |
| "Add New Event" | Modal title |
| "Modify your existing event." | Modal description |
| "Create a new event for your calendar." | Modal description |
| "Title" | Form label |
| "Enter a title" | Placeholder |
| "Variant" | Form label |
| "Select a variant" | Select placeholder |
| "Description" | Form label |
| "Enter a description" | Placeholder |
| "Cancel" | Button |
| "Save Changes" / "Create Event" | Button |

### 2. `src/components/filter.tsx`
| String | Context |
|--------|---------|
| "Clear Filter" | Dropdown menu item |

### 3. `src/components/StatusBadge.tsx`
| String | Context |
|--------|---------|
| Status values (available, rented, maintenance, upcoming, active, complete, cancelled) | Displayed via `charAt(0).toUpperCase() + status.slice(1)` |

### 4. `src/components/data-table/card-field-renderer.tsx`
| String | Context |
|--------|---------|
| "Not available" | Fallback text |

### 5. `src/features/reservations/reservation-detail-dialog.tsx`
| String | Context |
|--------|---------|
| "Reservation" | Dialog title |
| "Reservation details" | Dialog description |
| "total" / "day" | Price display |
| "Dropoff:" / "Pickup:" | Location labels |
| "Edit" | Button |
| "Close" | Button |

### 6. `src/features/reservations/reservations-edit-drawer.tsx`
| String | Context |
|--------|---------|
| "MIA" / "FLL" / "Other" | Location toggle values |

### 7. `src/features/autos/autos-mutate-dialog.tsx`
| String | Context |
|--------|---------|
| "Edit Auto" / "Add Auto" | Sheet title |
| "Name" | Form label |
| "Color" | Form label |
| "Select color" | Select placeholder |
| "License Plate" | Form label |
| "Delete" | Button |
| "Cancel" | Button |
| "Save" / "Add Auto" | Button |

### 8. `src/features/autos/autos-columns.tsx`
| String | Context |
|--------|---------|
| "None" | Fallback text |
| "Actions" | Column header |

### 9. `src/features/renters/renters-edit-drawer.tsx`
| String | Context |
|--------|---------|
| "Not available" | Phone fallback |
| "Close" | Button |

### 10. `src/components/ui/time-picker.tsx`
| String | Context |
|--------|---------|
| "Select a time" | Placeholder |

### 11. `src/components/ui/date-time-picker.tsx`
| String | Context |
|--------|---------|
| "MM/DD/YYYY hh:mm aa" | Time format display |

### 12. `src/components/ui/sidebar.tsx`
| String | Context |
|--------|---------|
| "Sidebar" | Sheet title |
| "Displays the mobile sidebar." | Sheet description |

### 13. `src/components/ui/responsive-modal.tsx`
| String | Context |
|--------|---------|
| "Close" | sr-only text |

### 14. `src/components/ui/dialog.tsx`
| String | Context |
|--------|---------|
| "Close" | sr-only text |

### 15. `src/components/settings.tsx`
| String | Context |
|--------|---------|
| "Calendar settings" | Dropdown menu label |
| "Date" / "Color" | Radio items |

### 16. `src/components/event-details-dialog.tsx`
| String | Context |
|--------|---------|
| "Responsible" | Label |

### 17. `src/components/layout/command-menu.tsx`
| String | Context |
|--------|---------|
| "Reservation" / "Auto" / "Renter" | Navigation URL map keys |

## Implementation Steps

1. **Update `en.json`** - Add all missing English strings under appropriate categories
2. **Update `es.json`** - Add corresponding Spanish translations
3. **Update components** - Replace hardcoded strings with `t()` function calls where needed

## Categories Structure (matching existing)
- `nav` - Navigation items
- `table` - Table headers
- `form` - Form placeholders/labels
- `command` - Command menu
- `empty` - Empty states
- `settings` - Settings panel
- `dialog` - Dialog titles
- `mutate` - CRUD operation messages
- `reporting` - Reporting section
- `metrics` - Metrics display
- `calendar` - Calendar section
- `profile` - Profile section
- `sidebar` - Sidebar section
- `aria` - ARIA labels
- `actions` - Action buttons
- `common` - Common strings
- `status` - Status badges
- `label` - Form labels
- `language` - Language selector
- `detail` - Detail pages
- `field` - Field sections
- `event` - Event-specific strings (NEW)
- `location` - Location values (NEW)
- `time` - Time picker (NEW)
- `autos` - Auto management (NEW)
- `renters` - Renter management (NEW)
- `reservation` - Reservation management (NEW)