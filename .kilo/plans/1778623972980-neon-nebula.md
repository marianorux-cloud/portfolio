# Mobile Experience Improvements - Builder Prompts

---

## Phase 1: Full-Width Table Search on Mobile

Update the DataTable toolbar component to use responsive width classes. Change the search input from a fixed width to full width on mobile.

**File:** src/components/data-table/toolbar.tsx
**Change:** Modify the Input className from `h-8 w-[150px] lg:w-[250px]` to `h-8 w-full md:w-[250px]`

---

## Phase 2: Mobile-Optimized Top Drawer Navigation

Create a mobile navigation drawer that opens from the top with hamburger menu trigger.

**Files:**
- Create src/components/layout/mobile-nav-drawer.tsx (new component)
- Modify src/components/layout/header.tsx (add hamburger trigger for mobile)
- Modify src/components/layout/app-sidebar.tsx (hide on mobile)

**Implementation:** Use the existing Sheet component positioned from top, populate with nav items from sidebarData.

---

## Phase 3: Reservation Details Dialog Updates

Fix button hierarchy in the reservation details dialog.

**File:** src/features/reservations/reservation-detail-dialog.tsx
**Changes:**
1. Swap button order: Close button should be first and primary (filled), Edit should be second and outline (secondary)
2. Add status badge next to the date in the dialog header

---

## Phase 4: Edit Reservation Form Updates

Remove visual clutter from time fields.

**File:** src/features/reservations/reservations-edit-drawer.tsx
**Change:** Remove clock icons from time picker fields.

---

## Phase 5: Autos Mobile Card Updates

Create mobile card component for autos matching reservation card structure.

**Files:**
- Create src/features/autos/auto-mobile-card-view.tsx
- Modify src/features/autos/autos-table.tsx

**Changes:** Remove footer and buttons, show color as description text, add status badge top-right of card header.

---

## Phase 6: Auto Details Drawer Updates

Update auto profile drawer with cleaner layout.

**Files:**
- src/features/autos/auto-profile-drawer.tsx
- src/locales/en.json
- src/locales/es.json

**Changes:**
1. Remove description text from header (no "Vehicle Details" line)
2. Change "Vehicle Details" translation to "Auto Details" in both en.json and es.json
3. Swap button order: Close should be primary (filled), Edit should be secondary (outline)

---

## Phase 7: Auto and Client Page Search Bar Updates

No changes needed - pages inherit the responsive search from Phase 1.

---

## Phase 8: Client Mobile Card Updates

Create mobile card component for clients matching reservation card structure.

**Files:**
- Create src/features/renters/client-mobile-card-view.tsx
- Modify src/features/renters/renters-table.tsx

**Changes:** Remove View button and footer, show "Not available" for N/A phones, use accent color for phone numbers, add reservation line if client has active reservation.

**Status:** Done

---

## Phase 9: Client Details Drawer Updates

Fix button hierarchy and remove description in client edit drawer.

**File:** src/features/renters/renters-edit-drawer.tsx
**Changes:**
1. Remove description field rendering (no "Client Details" line)
2. In view mode, swap button order: Close should be primary (filled), Edit should be secondary (outline)
3. Change "Done" button text to "Close"

**Status:** Done

---

## Phase 10: Auto Metrics Mobile Layout

Optimize auto metrics for mobile single-line display.

**File:** src/features/autos/autos-metrics.tsx
**Change:** Use flex-row layout so metrics display in one line on mobile.

**Status:** Done

---

**Updated Anchored Summary:**

## Goal
- Fix button hierarchy in reservation details dialog, create auto mobile card component, create client mobile card component, update auto profile drawer layout, fix client edit drawer, optimize auto metrics for mobile

## Constraints & Preferences
- (none)

## Progress
### Done
- Fixed button hierarchy in `reservation-detail-dialog.tsx`: Close (primary/filled) first, Edit (outline) second
- Added StatusBadge next to date in dialog header
- Created `auto-mobile-card-view.tsx` matching reservation card structure
- Modified `autos-table.tsx` to use AutoMobileCardView with renderMobileCard prop
- Removed CalendarIcon from `DatePicker.tsx` for cleaner layout
- Updated `auto-profile-drawer.tsx`: removed description text from header, swapped button order (Close primary, Edit outline)
- Updated `en.json` and `es.json`: changed "Vehicle Details" to "Auto Details"
- Created `client-mobile-card-view.tsx` matching reservation card structure
- Modified `renters-table.tsx` to use ClientMobileCardView with renderMobileCard prop
- Removed description in view mode, swapped button order (Close outline first, Edit primary second), changed "Done" to "Close" in `renters-edit-drawer.tsx`
- Changed `autos-metrics.tsx` to flex-row layout for mobile single-line display
- Updated `auto-mobile-card-view.tsx`: color + plate as "Color • Plate", reservation shows "Client until/on MMM d", hide cardContent when no reservation, clickable reservation line opens reservation details
- Updated `mobile-nav-drawer.tsx`: added Car icon logo with "AutoLola" text, increased text/icon sizes, accent color for current page, added Settings option
- Updated `client-mobile-card-view.tsx`: hide cardContent when no reservation, accent color link styling, "[auto name] until/on MMM d" format, clickable reservation opens details
- Updated `header.tsx`: moved hamburger to right, added search button on mobile

### In Progress
- (none)

### Blocked
- (none)

---

## Phase 11: Mobile Top Menu Updates

Update mobile navigation drawer with logo, larger text/icons, and accent color for current page.

**File:** src/components/layout/mobile-nav-drawer.tsx
**Changes:**
1. Add Car icon logo with "AutoLola" text at top left
2. Increase text size from `text-sm` to `text-base`
3. Increase icon size from `size-4` to `size-5`
4. Increase padding from `py-2` to `py-2.5`
5. Use accent color for current page

**Status:** Done

---

## Phase 13: Mobile Header Updates

Update mobile header with search button and move hamburger to right.

**Files:**
- src/components/layout/header.tsx
- src/components/layout/mobile-nav-drawer.tsx

**Changes:**
1. Add search button on mobile header
2. Move hamburger menu to right side of header
3. Add Settings option to mobile nav drawer

**Status:** Done