---
plan name: reservations-page-changes
plan description: Fix DateRangePicker styling and behavior, fix Combobox styling, remove toggle defaults, add validation
plan status: confirmed
---

## Final Decisions (from user)
- **Date picker styling**: Redo entire Dates field with proper shadcn styling
- **Date picker behavior**: Close only after BOTH dates selected
- **Toggle defaults**: No default, require explicit selection
- **Validation**: Use shadcn styling with error message "Required"

## Implementation Plan

### Phase 1: DateRangePicker Styling Fix
**File**: `src/components/DateRangePicker.tsx`
- Add `data-[empty=true]:text-muted-foreground` pattern to match shadcn styling
- Keep existing behavior (close when both dates selected)

### Phase 2: Combobox Validation (Already Done)
**File**: `src/components/ui/combobox.tsx`
- Already updated with `ChevronDown` icon, `text-muted-foreground` placeholder, no Check icon

### Phase 3: Fix Validation Display (Critical)
**File**: `src/features/reservations/reservations-edit-drawer.tsx`
- Replace `FormMessage` (5 instances: lines 380, 393, 412, 474, 520) with inline span:
  ```jsx
  {errors.fieldName && <span className="text-[0.8rem] font-medium text-destructive">Required</span>}
  ```

### Phase 4: Style Consistency Fix
**File**: `src/features/reservations/reservations-edit-drawer.tsx`
- Remove `shadow-none bg-transparent` from SelectTrigger (line 400) for consistent styling

## Current Issues Found

1. **FormMessage error**: The `FormMessage` component (lines 380, 393, 412, 474, 520 in reservations-edit-drawer.tsx) requires React Hook Form's `FormProvider` context, but the drawer doesn't use RHF. Using `useFormContext()` at line 45 in form.tsx will throw "Cannot destructure property 'getFieldState' from null or undefined value".

2. **Inconsistent styling**: The Auto Select has `shadow-none bg-transparent` (line 400) while other inputs don't. For shadcn consistency, all inputs should use the default shadow styling (no `shadow-none`).

3. **DateRangePicker styling**: Missing `data-[empty=true]:text-muted-foreground` pattern for proper placeholder styling when both dates are empty.

## Already Completed
1. **Combobox**: Already updated with `ChevronDown` (line 4), `text-muted-foreground` placeholder styling (line 69), and Check icon removed (not present).

2. **Toggle defaults**: `pickupLocation` and `dropoffLocation` already default to `''` (empty string) - lines 55 and 58.

3. **Validation state**: `errors` state exists (line 46) and validation logic is in place (lines 100-122).

## Remaining Work
1. `src/components/DateRangePicker.tsx` - Add `data-[empty=true]:text-muted-foreground` pattern
2. `src/features/reservations/reservations-edit-drawer.tsx`:
   - Replace all 5 `FormMessage` usages with inline `<span className="text-[0.8rem] font-medium text-destructive">Required</span>`
   - Remove `shadow-none bg-transparent` from SelectTrigger (line 400)